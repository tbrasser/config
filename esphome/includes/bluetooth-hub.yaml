substitutions:
  name: bluetooth-hub
esphome:
  name: ${name}-${location}
  project:
    name: esphome.bluetooth-proxy
    version: "1.0"
esp32:
  board: wemos_d1_mini32
  framework:
    type: esp-idf
improv_serial:
esp32_ble_tracker:
  scan_parameters:
    active: true
    interval: 1100ms
    window: 1100ms
bluetooth_proxy:
  active: true
